# kkrepo archive deployment configuration.
# Edit this file before starting the service.
#
# This file intentionally lists the main runtime properties so operators can
# discover what can be tuned without reading the source classpath defaults.
# Values here override the packaged application.properties.

# ---------------------------------------------------------------------------
# Application identity and optional Apollo integration
# ---------------------------------------------------------------------------

app.id=kkrepo
spring.application.name=kkrepo

# Apollo is disabled unless explicitly enabled before application bootstrap.
# To enable it for archive deployment, prefer environment variables or JVM args:
#   KKREPO_APOLLO_META=http://apollo-config:8080 bin/start.sh
#   JAVA_OPTS='-Dapollo.meta=http://apollo-config:8080' bin/start.sh

# ---------------------------------------------------------------------------
# HTTP server
# ---------------------------------------------------------------------------

server.port=8080
server.shutdown=graceful
server.servlet.context-path=/
server.tomcat.threads.max=100
server.tomcat.threads.min-spare=10
server.tomcat.max-connections=2000
server.tomcat.accept-count=200
server.tomcat.connection-timeout=30s
spring.mvc.async.request-timeout=10m

server.servlet.session.timeout=24h
server.servlet.session.cookie.max-age=-1
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.same-site=lax
server.servlet.session.cookie.secure=false

# ---------------------------------------------------------------------------
# Management and observability
# ---------------------------------------------------------------------------

management.server.port=8081
management.endpoints.web.exposure.include=health,metrics,prometheus
management.endpoint.health.probes.enabled=true
management.endpoint.health.show-details=never
management.metrics.tags.application=${spring.application.name}

# ---------------------------------------------------------------------------
# MySQL datasource and Flyway
# ---------------------------------------------------------------------------

spring.datasource.url=jdbc:mysql://127.0.0.1:3306/kkrepo?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
spring.datasource.username=kkrepo
spring.datasource.password=kkrepo
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.hikari.maximum-pool-size=50
spring.datasource.hikari.minimum-idle=10
spring.datasource.hikari.connection-timeout=5000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.leak-detection-threshold=30000
spring.datasource.hikari.transaction-isolation=TRANSACTION_READ_COMMITTED

spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=1
spring.flyway.validate-on-migrate=true
spring.flyway.out-of-order=false

# ---------------------------------------------------------------------------
# Spring runtime
# ---------------------------------------------------------------------------

spring.session.store-type=jdbc
spring.session.timeout=24h
spring.session.jdbc.initialize-schema=never
spring.session.jdbc.table-name=SPRING_SESSION
spring.session.jdbc.flush-mode=on_save

spring.servlet.multipart.max-file-size=1024MB
spring.servlet.multipart.max-request-size=1024MB
spring.jackson.default-property-inclusion=non_null
spring.jackson.time-zone=Asia/Shanghai
spring.lifecycle.timeout-per-shutdown-phase=30s
spring.task.scheduling.pool.size=4

# ---------------------------------------------------------------------------
# Nexus compatibility
# ---------------------------------------------------------------------------

kkrepo.compatibility.repository-url-prefix=/repository
kkrepo.repository.log-non-success-requests=true
kkrepo.repository.log-non-success-request-excluded-statuses=477,488

# ---------------------------------------------------------------------------
# Cache and catalog refresh
# ---------------------------------------------------------------------------

kkrepo.cache.backend=memory
kkrepo.cache.memory.maximum-size=500000
kkrepo.cache.version.local-ttl-seconds=2

kkrepo.cache.asset-metadata.enabled=true
kkrepo.cache.asset-metadata.ttl-seconds=60
kkrepo.cache.asset-metadata.missing-ttl-seconds=5
kkrepo.cache.repository-token.local-ttl-seconds=2
kkrepo.cache.npm-group-packument.enabled=true
kkrepo.cache.pypi-group-simple-index.enabled=true
kkrepo.cache.group-member-asset.enabled=true
kkrepo.cache.group-member-asset.ttl-seconds=60

# Short-TTL auth caches reduce repeated password/API-key lookups on CI traffic.
kkrepo.cache.basic-auth.enabled=true
kkrepo.cache.basic-auth.ttl-seconds=120
kkrepo.cache.basic-auth.missing-ttl-seconds=5
kkrepo.cache.api-key.enabled=true
kkrepo.cache.api-key.ttl-seconds=60
kkrepo.cache.api-key.missing-ttl-seconds=5

# Node-local immutable catalog snapshots. MySQL is the shared source of truth;
# cache_version is used as the cross-replica invalidation watermark.
kkrepo.catalog-cache.enabled=true
kkrepo.catalog-cache.refresh-interval-ms=60000
kkrepo.catalog-cache.initial-delay-ms=60000
# Supported values: mysql, none.
kkrepo.catalog-cache.broadcast-backend=mysql
kkrepo.catalog-cache.mysql.poll-delay-ms=500
kkrepo.catalog-cache.mysql.initial-delay-ms=500
kkrepo.catalog-cache.mysql.initial-jitter-ms=100

# ---------------------------------------------------------------------------
# Proxy repositories
# ---------------------------------------------------------------------------

kkrepo.proxy.negative-cache.enabled=true
kkrepo.proxy.negative-cache.ttl-minutes=5
# Supported values are Java HttpClient versions such as HTTP_1_1 and HTTP_2.
kkrepo.proxy.remote-http-version=HTTP_1_1
kkrepo.proxy.search-timeout-seconds=30
kkrepo.proxy.metadata-timeout-seconds=60
kkrepo.proxy.content-timeout-seconds=120
kkrepo.proxy.head-timeout-seconds=5
kkrepo.proxy.body-read-retry-attempts=1
kkrepo.maven.proxy-success-throttle-seconds=30

# ---------------------------------------------------------------------------
# Security
# ---------------------------------------------------------------------------

kkrepo.security.anonymous-read-enabled=false
kkrepo.security.token-header=X-Nexus-Plus-Token
kkrepo.nexus.legacy-ui.enabled=false

# Required outside dev/test profiles. Replace with strong, stable secrets before
# production use. Changing them after data is written can make encrypted blob
# store credentials, realm secrets, and API keys unreadable.
kkrepo.security.encryption.credential-secret=CHANGE_ME_credential_secret_at_least_32_chars
kkrepo.security.encryption.api-key-payload-secret=CHANGE_ME_api_key_payload_secret_at_least_32_chars

kkrepo.security.default-authenticated-role-id=nx-anonymous
kkrepo.security.authorization-cache.enabled=true
kkrepo.security.authorization-cache.ttl-minutes=10
kkrepo.security.authorization-cache.version-local-ttl-seconds=2

kkrepo.security.hsts-enabled=false
# Set this when the service is behind a reverse proxy and OIDC redirect URLs
# must use the public URL, for example https://nexus.example.com.
kkrepo.security.external-base-url=
kkrepo.security.csrf.cookie-secure=false

# Outbound policy applies to proxy repository upstream URLs and selected admin
# connectivity checks. Keep private addresses disabled for internet-facing use.
kkrepo.security.outbound.allow-private-addresses=false
kkrepo.security.outbound.allowed-hosts=
kkrepo.security.forwarded-headers.trusted-proxies=

kkrepo.security.rate-limit.login-per-minute=20
kkrepo.security.rate-limit.bootstrap-per-minute=5
kkrepo.security.authentication-ticket-ttl-seconds=300
kkrepo.security.authentication-ticket-cleanup-interval-ms=60000
kkrepo.security.authentication-ticket-cleanup-initial-delay-ms=60000
kkrepo.security.upload.max-request-bytes=1073741824

# ---------------------------------------------------------------------------
# Blob storage
# ---------------------------------------------------------------------------

# S3/OSS client tuning. Actual blob store endpoint, bucket, credentials, and
# engine are configured in /admin/ Blob Store.
kkrepo.storage.s3.max-connections=512
kkrepo.storage.s3.connection-timeout-ms=5000
kkrepo.storage.s3.socket-timeout-ms=120000
kkrepo.storage.s3.connection-acquisition-timeout-ms=10000
kkrepo.storage.s3.tcp-keep-alive=true
kkrepo.storage.s3.multipart-threshold-bytes=67108864
kkrepo.storage.s3.multipart-part-size-bytes=16777216
kkrepo.storage.s3.multipart-concurrency=4
kkrepo.blob.transfer-buffer-size-bytes=1048576

# File blob store is useful for local trial. In production, prefer OSS/S3. File
# blob stores are valid in distributed deployments only when every replica mounts
# the same strong-consistency shared filesystem.
kkrepo.storage.file.enabled=true
kkrepo.storage.file.base-dir=${KKREPO_HOME}/data/blobs
kkrepo.storage.file.production-enabled=false
kkrepo.storage.file.shared-filesystem=false
kkrepo.storage.file.temp-cleanup-enabled=true
kkrepo.storage.file.temp-cleanup-interval-ms=300000
kkrepo.storage.file.temp-cleanup-grace-seconds=86400

# ---------------------------------------------------------------------------
# Background workers
# ---------------------------------------------------------------------------

kkrepo.repository-index-rebuild.enabled=true
kkrepo.repository-index-rebuild.poll-interval-ms=1000
kkrepo.repository-index-rebuild.batch-size=16

kkrepo.maven.metadata-rebuild.enabled=true
kkrepo.maven.metadata-rebuild.poll-interval-ms=1000
kkrepo.maven.metadata-rebuild.batch-size=32
kkrepo.maven.sync-metadata-rebuild=false
kkrepo.maven.runtime-cache-ttl-seconds=30

kkrepo.pypi.index.sync-wait-ms=0

kkrepo.blob-gc.enabled=true
kkrepo.blob-gc.poll-interval-ms=30000
kkrepo.blob-gc.batch-size=64
kkrepo.blob-gc.reconcile-scan-batch-size=1024
kkrepo.blob-gc.reconcile-mark-batch-size=256
kkrepo.blob-gc.deleted-grace-seconds=3600
kkrepo.blob-gc.claim-retry-seconds=600

# Retry transient MySQL transaction errors in selected operations.
kkrepo.persistence.transaction-retry.max-attempts=3
kkrepo.persistence.transaction-retry.initial-backoff-ms=25

# ---------------------------------------------------------------------------
# Logging
# ---------------------------------------------------------------------------

logging.file.name=${KKREPO_HOME}/logs/kkrepo.log
logging.level.root=INFO
logging.level.com.github.klboke.kkrepo=INFO
