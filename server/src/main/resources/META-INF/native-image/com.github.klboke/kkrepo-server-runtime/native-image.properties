# JDK 25's password helper caches System.console() in this holder. Initializing it while
# building a native image captures the build container's ProxyingConsole in the image heap.
# Resolve the console at application startup instead so the native process sees its own runtime.
Args = --initialize-at-run-time=sun.security.util.Password$ConsoleHolder
